<% provide(:title, "Home") %>

<% if logged_in? %>
    <i class="material-icons toggle-modal-button" id="show-post-form">add_circle_outline</i>
    <%= render 'shared/post_form' %>
<% end %>
<% if User.first.posts.any? %>
  <div class="feed">
    <ol>
      <%= render @posts %>
    </ol>
    <% will_paginate @posts %>
  </div>
<% end %>
