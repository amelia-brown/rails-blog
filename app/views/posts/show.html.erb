<% if logged_in? %>
  <i class="material-icons modal__toggle-button modal__toggle-button--open">edit</i>
  <%= render 'shared/edit_post_form' %>
<% end %>
<div class="post-show" id="post-<%= @post.id %>">
  <h1 class="post-title">
    <%= @post.title %>
  </h1>
  <div class="post-photo"><img src="<%= @post.picture.url if @post.picture %>" /></div>
  <div class="post-info">
    <h3 class="post-byline">
      by <%= @post.user.name %>
  </h3>
  <span class="post-date">
    <%= date = post_time %>
  </span>
  </div>
  <div class="post-content">
    <%= @post.content.html_safe %>
  </div>
  <% if @previous_posts.count > 0 %>
    <div class="post-more">
      <p>Recently posted:</p>
      <ul class="post-more-list">
        <% @previous_posts.each do |post| %>
          <li class="post-more-item">
            <%= link_to post do %>
              <span class="post-more-title"><%= post.title %></span>
              <div class="post-more-picture">
                <img src="<%= post.picture.url if post.picture %>" />
              </div>
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>
  <% end %>
</div>

