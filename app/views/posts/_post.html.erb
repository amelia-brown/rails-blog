<li id="post-<%= post.id %>">
  <span class="title"></span>
  <p class="byline"><% post.user.name %></p>
  <span class="timestamp">Posted <%= time_ago_in_words(post.created_at) %> ago.</span>
  <span class="content"><%= post.content %></span>
</li>
